# UltraIa environment — copy to `.env` (root, for Prisma CLI) and `apps/web/.env` (for Next.js)

# SQLite in dev; switch to Postgres URL in production (e.g. postgresql://user:pass@host:5432/ultraia)
DATABASE_URL="file:./dev.db"

# LLM provider (OpenAI-compatible by default via the Vercel AI SDK)
# ULTRAIA_PROVIDER controla qué proveedor usa el gateway:
#   openai  -> necesita OPENAI_API_KEY
#   google  -> necesita GOOGLE_API_KEY (Gemini, tier gratuito)
#   deepseek-> necesita DEEPSEEK_API_KEY (tier gratuito en platform.deepseek.com)
#   ollama  -> SIN KEY: modelo local (Llama/Phi). Requiere Ollama corriendo en
#              http://localhost:11434 (OLLAMA_BASE_URL opcional).
# ULTRAIA_PROVIDER controla qué proveedor usa el gateway.
#   ollama  -> SIN KEY: modelo local. Requiere Ollama corriendo en
#              http://localhost:11434 (OLLAMA_BASE_URL opcional). Es el valor por defecto.
#   openai  -> necesita OPENAI_API_KEY
#   google  -> necesita GOOGLE_API_KEY (Gemini, tier gratuito)
#   deepseek-> necesita DEEPSEEK_API_KEY (tier gratuito en platform.deepseek.com)
ULTRAIA_PROVIDER="ollama"

# Modelo por defecto (usado cuando un agente no fija el suyo).
ULTRAIA_MODEL="llama3.1"

OPENAI_API_KEY=""

# Proveedores alternativos (gratuitos, requieren su key)
GOOGLE_API_KEY=""
DEEPSEEK_API_KEY=""
DEEPSEEK_BASE_URL="https://api.deepseek.com/v1"

# Ollama (opcional, clave local sin API externa)
OLLAMA_BASE_URL="http://localhost:11434/v1"

# Default model used for blueprint generation, judging and agents when a blueprint
# does not pin its own model. Examples: gpt-4o-mini, gpt-4.1-mini, gpt-5-mini, claude-3-5-haiku
ULTRAIA_MODEL="gpt-4o-mini"

# Public origin, used for links/redirects
APP_URL="http://localhost:3000"
