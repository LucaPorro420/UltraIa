# Variables de ejemplo para sacd_system (NO es el .env del proyecto)
# Copiar a .env y completar. Sin OPENAI_API_KEY el script corre en modo
# fallback determinista (no usa LLM real).
OPENAI_API_KEY=
OPENAI_API_BASE=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini